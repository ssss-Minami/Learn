1.SPI模式时，空闲时CLK为高电平，数据在第二个跳变沿采集，
   应CPHA=1, CPOL=1
   每次发数据需要两个或者更多个8位字节，第一个字节第一位为读（1）（reg | 0x80）写（0）（reg&0x7F），余下为地址
   第二个字节开始为数据，高位在前
2.内置ADC位数为16位，若采样范围为+-2g ，则精度为2^16/4 =16384LSB，ADC寄存器内的原始数据需/16382得到真实数据